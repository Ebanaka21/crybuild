# 🚀 Быстрый старт проекта OBI Clone

## Предварительные требования

- PHP 8.2+
- Composer
- Node.js & npm
- MySQL 8.0+ (или SQLite)

## Установка за 5 минут

### 1. Установка зависимостей

```bash
cd e:\OSPanel\home\obi-company\obi

# Установить PHP зависимости
composer install

# Установить JavaScript зависимости
npm install
```

### 2. Настройка файла .env

Файл `.env` уже готов. Проверьте параметры БД:

```env
APP_NAME=OBI
APP_ENV=local
APP_DEBUG=true
APP_URL=http://localhost:8000

DB_CONNECTION=sqlite
# или используйте MySQL:
# DB_CONNECTION=mysql
# DB_HOST=127.0.0.1
# DB_PORT=3306
# DB_DATABASE=obi
# DB_USERNAME=root
# DB_PASSWORD=
```

### 3. Миграции и сидеры

```bash
# Выполнить миграции (создать таблицы)
php artisan migrate

# Заполнить БД тестовыми данными
php artisan db:seed
```

### 4. Компиляция ассетов

```bash
# Для разработки (с автоперезагрузкой)
npm run dev

# Или для продакшена
npm run build
```

### 5. Запуск сервера

В отдельном терминале:

```bash
php artisan serve
```

### 6. Открыть в браузере

```
http://localhost:8000
```

## 🔑 Тестовые учетные записи

После выполнения `php artisan db:seed` доступны:

| Email | Пароль | Роль |
|-------|--------|------|
| admin@example.com | password123 | Администратор |
| user@example.com | password123 | Пользователь |

## 📝 Что было исправлено

### Исправленные ошибки:
- ✅ Ошибка login: `InvalidArgumentException - Unable to locate a class or view for component [guest-layout]`
  - **Решение:** Создан компонент `resources/views/components/guest-layout.blade.php`

- ✅ Ошибка product: `Undefined variable $_instance`
  - **Решение:** Удалены Livewire функции, переделано на AJAX

- ✅ Кнопка "В корзину" не работала
  - **Решение:** Замена `wire:click` на обычный `onclick` с AJAX обработчиками

### Улучшения:
- 🎨 Обновлены страницы логина и регистрации (новый дизайн с логотипом)
- 🗑️ Удалены отзывы со страницы товара
- ⚡ Добавлено кэширование для оптимизации производительности
- 🟠 Footer изменён на оранжевый цвет (bg-orange-600)
- 🔧 Приведены в порядок seeders с информативными сообщениями

## 🌐 API Endpoints

Добавлены новые AJAX endpoints:

```
POST /api/cart/add
- Параметры: product_id, quantity
- Добавляет товар в корзину

POST /api/wishlist/toggle
- Параметры: product_id
- Добавляет/удаляет товар из избранного (требует авторизации)

POST /api/reviews/create
- Параметры: product_id, rating, comment
- Создает отзыв на товар (требует авторизации)
```

## 🔧 Полезные команды

```bash
# Очистить кэш
php artisan cache:clear
php artisan config:clear

# Пересоздать БД с тестовыми данными
php artisan migrate:fresh --seed

# Создать администратора Filament (если нужно)
php artisan filament:user

# Просмотр всех маршрутов
php artisan route:list
```

## 📁 Основные файлы

- `routes/web.php` - Маршруты и API endpoints
- `resources/js/app.js` - JavaScript функции для корзины и wishlist
- `resources/views/components/guest-layout.blade.php` - Layout для auth страниц
- `resources/views/pages/product.blade.php` - Страница товара (без отзывов)
- `resources/views/layouts/footer.blade.php` - Footer (оранжевый)
- `database/seeders/DatabaseSeeder.php` - Главный seeder

## ❓ Часто задаваемые вопросы

**Q: Кнопка в корзину не работает**
A: Убедитесь, что:
1. Запущен `npm run dev` для компиляции JavaScript
2. Функция `addToCart` загружена (проверьте консоль браузера)
3. CSRF токен присутствует в meta тегах

**Q: Страница товара загружается медленно**
A: Кэширование включено. Первый запрос может быть медленнее, последующие будут быстрее.

**Q: Как добавить новый товар?**
A: Через админку: http://localhost:8000/admin/resources/products

**Q: SQLite или MySQL?**
A: По умолчанию используется SQLite для простоты разработки. Для продакшена переключитесь на MySQL в .env

---

**Проект готов к работе! 🎉**
