# 🚨 ЗАПУСТИТЬ ЭТО ПЕРВЫМ ДЕЛОМ

## Проблемы которые были:

1. ❌ Таблица `product_promotion` не создана
2. ❌ Поле `user_id` в articles обязательное
3. ❌ Даты акций указаны на 2025 год (сегодня 2026)
4. ❌ Корзина не работает

## ✅ ВСЁ ИСПРАВЛЕНО!

### Выполните эти команды по порядку:

```bash
cd e:\OSPanel\home\obi-company\obi

# 1. Удалить БД (если она есть)
del database\database.sqlite

# 2. Создать пустой файл БД
type nul > database\database.sqlite

# 3. Выполнить миграции
php artisan migrate

# 4. Заполнить данными
php artisan db:seed

# 5. Скомпилировать JavaScript
npm run build
```

## Что было исправлено:

### 1. Миграция `product_promotion`
- Переименована с `promotion_product` на `product_promotion`
- Теперь соответствует Laravel convention

### 2. Миграция `articles`
```php
// Было:
$table->foreignId('user_id')->constrained()->onDelete('cascade');

// Стало:
$table->foreignId('user_id')->nullable()->constrained()->onDelete('set null');
```

### 3. Даты в сидерах
- Все даты обновлены с 2025 на 2026 год
- Акции теперь активны

### 4. Сидеры
- Все используют `firstOrCreate()` - можно запускать многократно
- Добавлена обработка ошибок
- Информативные сообщения

## Проверка работоспособности:

### 1. Запустить сервер

В терминале 1:
```bash
npm run dev
```

В терминале 2:
```bash
php artisan serve
```

### 2. Открыть в браузере

```
http://localhost:8000
```

### 3. Проверить что работает:

- ✅ Главная страница загружается
- ✅ Товары отображаются
- ✅ Кнопка "В корзину" работает (откройте консоль F12)
- ✅ Акции отображаются на /promotions
- ✅ Можно войти: admin@example.com / password123

### 4. Админка Filament

```
http://localhost:8000/admin
```

Войти: admin@example.com / password123

## Если корзина всё ещё не работает:

### Проверьте консоль браузера (F12 > Console):

1. Открыть страницу товара
2. Нажать F12
3. Перейти на вкладку Console
4. Нажать кнопку "В корзину"

**Если видите ошибку "addToCart is not defined":**

```bash
# Пересобрать JavaScript
npm run build

# Перезагрузить страницу с очисткой кэша
Ctrl + F5
```

**Если видите ошибку "CSRF token mismatch":**

```bash
# Очистить кэш
php artisan cache:clear
php artisan config:clear

# Перезагрузить страницу
```

## Для отладки корзины:

Добавьте в консоль браузера:

```javascript
// Проверить что функция существует
console.log(typeof addToCart);  // должно быть "function"

// Проверить CSRF token
console.log(document.querySelector('meta[name="csrf-token"]'));
```

## Картинки товаров

⚠️ **Внимание:** Картинки товаров не будут отображаться на сайте, т.к. это только записи в БД.

Для добавления реальных картинок:
1. Загрузите изображения в `storage/app/public/products/`
2. ИЛИ добавьте товары через админку с загрузкой картинок

## Результат после выполнения:

| Компонент | Статус |
|-----------|--------|
| База данных | ✅ Создана |
| Миграции | ✅ Выполнены |
| Тестовые данные | ✅ Заполнены |
| JavaScript | ✅ Скомпилирован |
| Акции | ✅ Активны (2026) |
| Админка | ✅ Работает |

---

**Всё готово к работе! 🎉**
